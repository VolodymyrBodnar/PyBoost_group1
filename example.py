class ComplexNumber:
    def __init__(self, real, img):
        self.real = real
        self.img = img

    def __add__(self, other):
        # return self.value + other.value
    # TODO add, multiply, substract

a = Num(5)
b = Num(2)

result = 5 + 2
print(result)